[Category("Filter")]
[Summary("")]
shader OCIOTransform_TextureFX : FilterBase
{
    stage compose OCIOBase Display;

    float4 Filter(float4 tex0col)
    {
        return Display.OCIODisplay(tex0col);
    }
};